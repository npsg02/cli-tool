# {{PROJECT_NAME}}

A CLI tool built with Bash.

## Installation

```bash
chmod +x main.sh
```

## Usage

```bash
./main.sh help
./main.sh version
```

## Building

This is a bash script, so no compilation is needed. Just make it executable:

```bash
chmod +x main.sh
```

To install to bin directory:
```bash
cp main.sh ../../bin/{{PROJECT_NAME}}
chmod +x ../../bin/{{PROJECT_NAME}}
```
